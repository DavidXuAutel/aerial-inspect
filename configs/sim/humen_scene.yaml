# Avant-AirSim HumenCorridor (虎门大桥) on 84 — CARLA+AirSim co-sim.
scene_id: humen_corridor
map_role: humen_bridge_corridor
renderer: avant_airsim
carla_map: HumenCorridor

airsim_host_local: 127.0.0.1
airsim_host_public: 10.229.20.84
airsim_bind_ip: 0.0.0.0
airsim_port: 41463
carla_port: 2200

# Avant-AirSim default multirotor + RGB camera (not OpenFly drone_1/front_custom)
vehicle: SimpleFlight
camera: "0"

# Main-span tower area, visually confirmed via "虎门大桥" sign + nadir deck-line bearing.
# AirSim-native coords (CARLA/OpenDRIVE frame differs — do not reuse those numbers here).
# Offset 80m off-axis from centroid [2400,-20,80] so spawn isn't inside the tower legs.
spawn_xyz: [2365.0, 55.0, 90.0]
spawn_yaw_deg: -64.0

# SEARCH / scene-check alternate: pull back along corridor toward shore
alt_spawn_xyz: [2150.0, 260.0, 95.0]
alt_spawn_yaw_deg: -64.0
